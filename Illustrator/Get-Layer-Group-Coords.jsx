var locations = [];var doc = app.activeDocument;  var originalLayer = doc.activeLayer;  function test(){      var newLayer, thisGroup, thisContent;      for(var i=originalLayer.groupItems.length - 1; i > -1; i--){          var thisGroup = originalLayer.groupItems[i];          var x = thisGroup.left + thisGroup.width / 2;        var y = thisGroup.top + thisGroup.height;        locations.push({"x": x, "y": y});    };       }function logMe(input){     var now = new Date();     var output = input;     $.writeln(output);     var logFile = File("~/Desktop/" + doc.name+ originalLayer + ".txt");     logFile.open("e");     logFile.writeln(output);     logFile.close();     alert ('Coords Like a Boss \n'+output + '\n\n\n Just copy these the old fashion way \n\n A file with this JS Object has been saved to your Desktop');}  test();logMe(objToString(locations)); function objToString (obj) {    var str = '{';    if(typeof obj=='object')      {        for (var p in obj) {          if (obj.hasOwnProperty(p)) {              str += p + ':' + objToString (obj[p]) + ',';          }      }    }      else      {         if(typeof obj=='string')          {            return '"'+obj+'"';          }          else          {            return obj+'';          }      }    return str.substring(0,str.length-1)+"}";}